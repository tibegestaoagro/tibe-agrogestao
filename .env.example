# ─────────────────────────────────────────────────────────────
# Tibé (AgroGestão): variáveis de ambiente
# Copie este arquivo para .env (local) e preencha os valores.
# Nunca commite .env: apenas este .env.example é versionado.
# Referência: PRD seção 10.2
# ─────────────────────────────────────────────────────────────

# Banco de dados (Neon.tech, PostgreSQL 17)
# Connection string com SSL. Use a "pooled" para a app e a "direct" para migrations se desejar.
DATABASE_URL=

# NextAuth v5
# NEXTAUTH_SECRET: gere com `openssl rand -base64 32`
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Sessão da plataforma interna (Módulo 6, PlatformUser): instância NextAuth
# separada da de tenant acima, com secret próprio de propósito (defense in
# depth: mesmo que um token vaze, não é válido para a outra instância).
# Gere com `openssl rand -base64 32` (use um valor DIFERENTE do NEXTAUTH_SECRET).
PLATFORM_AUTH_SECRET=

# Identidade por token do aplicativo (Onda 1): assina o access token JWT de 15
# minutos que o app envia em `Authorization: Bearer` para as rotas /api/v1.
# Gere com `openssl rand -base64 32` e use um valor DIFERENTE de
# NEXTAUTH_SECRET e de PLATFORM_AUTH_SECRET: as três identidades precisam ser
# independentes (o código recusa iniciar se este for igual ao NEXTAUTH_SECRET).
MOBILE_JWT_SECRET=

# Redis Cloud (BullMQ: fila de jobs)
REDIS_URL=

# WhatsApp Business Cloud API (Meta)
META_WHATSAPP_TOKEN=
META_WHATSAPP_PHONE_ID=
META_WHATSAPP_VERIFY_TOKEN=

# O APLICATIVO não precisa de variável do n8n (2026-07-30). Ele orquestra
# apenas a ENTRADA do agente WhatsApp (recebe a mensagem, classifica a intenção
# com a OpenAI e chama /api/internal/whatsapp/*), autenticando-se com
# INTERNAL_API_SECRET abaixo. O envio de mensagens e de alertas é do próprio
# Tibé, pelo provider ativo em WhatsAppProviderConfig (Módulo 7).
# Ver docs/n8n-whatsapp-workflow.md.
#
# As duas abaixo são do BANCO DE PROVAS (`npm run wa`, scripts/whatsapp-e2e.ts),
# não do aplicativo: ele conversa com o agente real e lê a resposta por
# programa. Sem elas o comando falha na primeira linha, e é assim que a falta
# aparece. Ficaram fora deste arquivo até 2026-08-24, embora o código as leia.
# URL_N8N: base do n8n; o webhook usado é <base>/webhook/atendimento.
URL_N8N=
# WA_TEST_PHONE: telefone do usuário de teste, só dígitos, com DDI. Ele sai
# pronto no fim do `npm run wa:seed`. Trocar por engano faz a limpeza do script
# apagar o histórico de outro número, então confira antes.
WA_TEST_PHONE=

# Asaas (cobrança)
ASAAS_API_KEY=
ASAAS_ENV=sandbox
# Token que você mesmo escolhe e cadastra no painel do Asaas ao criar o
# webhook (Configurações > Integrações > Webhooks): o Asaas ecoa esse valor
# no header `asaas-access-token` em toda notificação, para autenticação.
ASAAS_WEBHOOK_TOKEN=

# Cloudflare R2 (storage)
CLOUDFLARE_R2_ACCESS_KEY=
CLOUDFLARE_R2_SECRET_KEY=
CLOUDFLARE_R2_BUCKET=

# Secret para rotas internas (/api/internal/*)
INTERNAL_API_SECRET=

# Chave que assina o link de relatório financeiro (HMAC). PRECISA ser diferente
# de INTERNAL_API_SECRET: o link é feito para ser mandado por WhatsApp e aberto
# sem sessão, então circula em conversa e em captura de tela. Enquanto as duas
# eram a mesma chave (até 2026-08-20), vazar um link entregava junto a
# credencial que escreve em qualquer tenant.
# Sem esta variável o sistema ainda funciona, usando INTERNAL_API_SECRET como
# reserva e avisando no log, mas isso é transição, não desenho.
REPORT_LINK_SECRET=

# Vercel Cron: valor do Authorization: Bearer enviado automaticamente pela
# Vercel nas rotas de cron (configuradas em vercel.json). Defina o mesmo valor
# em Project Settings > Environment Variables: a Vercel injeta o header sozinha.
CRON_SECRET=

# Chave AES-256-GCM (32 bytes, base64) para criptografar credenciais de
# provider WhatsApp no banco. Gere com:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
CONFIG_ENCRYPTION_KEY=

# Email (arquitetura 2026-07-29): canal adicional ao WhatsApp para boas-vindas
# e notificações de automação (Alert). EMAIL_PROVIDER escolhe o adapter ativo
# ("gmail_smtp" ou "resend"); default é "gmail_smtp" quando não definido.
EMAIL_PROVIDER=gmail_smtp

# Gmail SMTP (ativo em desenvolvimento/início de produção, enquanto o domínio
# próprio não está pronto para o Resend). GMAIL_SMTP_USER é o email completo;
# GMAIL_SMTP_APP_PASSWORD é uma "Senha de app" do Google (não a senha normal
# da conta, exige verificação em duas etapas ativada):
# https://myaccount.google.com/apppasswords
GMAIL_SMTP_USER=
GMAIL_SMTP_APP_PASSWORD=

# Resend (fica pronto, mas inativo até EMAIL_PROVIDER=resend e o domínio de
# envio estar verificado no painel do Resend).
RESEND_API_KEY=
RESEND_FROM_EMAIL=

# Push web (Onda 2, seam de notificação em src/lib/notify): terceiro canal de
# alerta, gratuito, além de WhatsApp/email. Gere o par com
# `npx web-push generate-vapid-keys` (pacote "web-push" já instalado) e nunca
# reaproveite um par de outro ambiente: cada instalação tem o próprio.
# VAPID_PUBLIC_KEY é enviada ao navegador em runtime (GET
# /api/v1/notifications/public-key), não é segredo; VAPID_PRIVATE_KEY nunca
# sai do servidor.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
# Contato do responsável pela aplicação, formato "mailto:" (exigido pelo
# protocolo Web Push para o push service poder avisar em caso de abuso).
VAPID_SUBJECT=
